<%= form_for :project, url:  projects_path, remote: true do |f| %>

    <p>
      <%= f.label :name %><br>
      <%= f.text_field :name, maxlength: 30 %>
    </p>
  
    <p>
      <%= f.submit %>
    </p>
  <% end %>
